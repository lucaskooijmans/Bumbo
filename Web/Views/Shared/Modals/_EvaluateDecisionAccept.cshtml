@model Web.ViewModels.AbsenceViewModel

<dialog id="showAccept@(Model.Id)" class="modal">
    <div class="modal-box">
        <form method="dialog">
            <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">
                <i class="fa-solid fa-x"></i>
            </button>
        </form>
        <h2 class="font-bold text-lg">Weet je zeker dat je het verlofverzoek wilt goedkeuren?</h2>
        <div class="modal-action">
            <form asp-controller="Absence" asp-action="Evaluate" asp-route-id="@Model.Id" asp-route-status="Approved">
                <button type="submit" class="btn btn-success"><i class="fa-solid fa-check"></i>goedkeuren</button>
            </form>
        </div>
    </div>
</dialog>