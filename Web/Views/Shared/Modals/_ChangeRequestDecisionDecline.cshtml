@model Web.ViewModels.ShiftViewModel

<dialog id="showDeclineChangeRequest@(Model.Id)" class="modal">
    <div class="modal-box">
        <form method="dialog">
            <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">
                <i class="fa-solid fa-x"></i>
            </button>
        </form>
        <h2 class="font-bold text-lg">Weet je zeker dat je deze dienst wilt afwijzen?</h2>
        <span>
            @Model.Start.DateName() | @Model.Start.Time() - @Model.End.Time()
        </span>
        <div class="modal-action">
            <form asp-controller="EmployeeShift" asp-action="AcceptDenyChangeRequest" asp-route-id="@Model.Id">
                <input type="hidden" name="AcceptDeny" value="false"/>
                <button type="submit" class="btn btn-error"><i class="fa-solid fa-x"></i>Afwijzen</button>
            </form>
        </div>
    </div>
</dialog>